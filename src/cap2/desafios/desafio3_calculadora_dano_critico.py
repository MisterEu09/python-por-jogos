dano = int(input("Dano base: "))
multiplicador = float(input("Multiplicador crítico: "))
dano_final = dano * multiplicador
print(f"DANO CRÍTICO: {int(dano_final)}!")
# Exemplo de entrada:
# Dano base: 50
# Multiplicador crítico: 2.5
# Saída esperada: DANO CRÍTICO: 125!
